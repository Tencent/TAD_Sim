package main

import "txsim/cli/cmd"

// The main function is the entry point to the program
func main() {
	// Call the Execute function of the cmd package to
	// execute the logic of the command line utility
	cmd.Execute()
}
