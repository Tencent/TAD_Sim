{
  "name": "simapp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "scene-editor",
    "map-editor"
  ],
  "scripts": {
    "dev:scene": "cd scene-editor && npm run dev",
    "dev:desktop": "cd desktop && npm run dev && cd ..",
    "dev:map": "cd map-editor && npm run dev",
    "build:scene": "cd scene-editor && npm run build",
    "build:desktop": "cd desktop && npm run build && cd ..",
    "build:map": "cd map-editor && npm run build",
    "lint": "eslint --fix ."
  },
  "dependencies": {},
  "devDependencies": {
    "@antfu/eslint-config": "2.18.0",
    "@types/node": "^20.8.10",
    "eslint": "^8.57.0",
    "typescript": "^5.0.2"
  }
}
