{
  "name": "simapp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "scene-editor",
    "map-editor"
  ],
  "scripts": {
    "dev:scene": "npm run dev -w scene-editor",
    "dev:desktop": "cd desktop && npm run dev && cd ..",
    "dev:map": "npm run dev -w map-editor",
    "build": "build.bat",
    "build:scene": "npm run build -w scene-editor",
    "build:desktop": "cd desktop && npm run build && cd ..",
    "build:map": "npm run build -w map-editor",
    "lint": "eslint --fix ."
  },
  "dependencies": {},
  "devDependencies": {
    "@antfu/eslint-config": "2.18.0",
    "@types/node": "^20.8.10",
    "eslint": "^8.57.0",
    "typescript": "^5.0.2"
  }
}